
server:
  port: 7777

spring:
  datasource:
    url: jdbc:mysql://mysql-container/ifa_project?allowpublickeyretrieval=true&useSSl=false&serverTimezone=UTC
    username: eddi
    driver-class-name: com.mysql.cj.jdbc.Driver
    dialect: org.hibernate.dialect.MySQL8Dialect
    password: eddi@123

  jpa:
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
    database: mysql
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update